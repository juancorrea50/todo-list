const addItemBtn = ()=>{
    //DOM creation
    const addBtn = document.createElement('button');
    //Text for button
    addBtn.textContent = '+';
    //Append the button to it's container
    
    return addBtn;
}
export default addItemBtn;